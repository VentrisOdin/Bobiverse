def cli(args, orch_url: str) -> None:
    """
    Placeholder for future python-exec task support.

    This command is intentionally a NO-OP until the orchestrator
    implements /tasks/python or equivalent.
    """
    print("[submit-python] Not implemented yet.")
    print("  Orchestrator does not currently expose a /tasks/python endpoint.")
    print("  This is a placeholder so `bobctl submit-python` doesn't break.")
    print("  When you're ready, we can wire this to a real python-exec route.")
